import React from "react";
import { Box } from "@mui/material";

const Body: React.FC = () => {
  return (
    <Box sx={{ py: 8 }}>{/* Content will be added later if needed */}</Box>
  );
};

export default Body;
